# 对话上下文感知与压缩 —— 技术实现文档

> 对应产品方案：`doc/产品需求/对话上下文感知与压缩_产品优化方案.md`

---

## 一、实现概览

| 类型 | 文件 | 操作 |
|------|------|------|
| 新增 | `app/context_compressor.py` | 上下文压缩模块 |
| 修改 | `config/settings.py` | 新增配置项 |
| 修改 | `app/chat_service.py` | 答案生成前调用 compress_context |
| 修改 | `services/rag/pipeline.py` | 答案生成前调用 compress_context |
| 修改 | `services/rag/langchain_chain.py` | 答案生成前调用 compress_context |
| 可选 | `app/answer_engine.py` | Prompt 历史上下文说明微调 |

---

## 二、新增文件：app/context_compressor.py

**路径**：`app/context_compressor.py`

**职责**：对 Redis 取出的完整对话历史进行筛选与压缩，产出供 `generate_answer` 使用的精简上下文。

### 2.1 完整代码

```python
"""
对话上下文压缩模块 - 筛选与截断历史对话，控制传入 LLM 的 token 量

在答案生成前调用，对 Redis 取出的 context 进行：
1. 关联性筛选（recent_only / hybrid + 关键词相似度）
2. 单轮截断（query/answer 字数限制）
3. Token 预算裁剪
"""
import re
from typing import Any, Dict, List, Optional

from config import settings

# 配置项（带默认值，避免 settings 未配置时报错）
def _get(key: str, default: Any) -> Any:
    return getattr(settings, key, default)


def compress_context(
    query: str,
    context: List[Dict[str, Any]],
    *,
    mode: Optional[str] = None,
    rewritten_query: Optional[str] = None,
) -> List[Dict[str, Any]]:
    """
    对原始 context 进行筛选与压缩，返回可用于答案生成的上下文。

    Args:
        query: 当前用户问题
        context: Redis 取出的完整历史 [{"query":"...", "answer":"..."}, ...]
        mode: 覆盖配置的筛选模式
        rewritten_query: 改写后的问题，用于筛选时计算相似度（可选，更准确）

    Returns:
        筛选并截断后的 [{"query":"...", "answer":"..."}, ...]
    """
    if not _get("CONTEXT_COMPRESSION_ENABLED", True):
        return context

    if not context:
        return []

    selection_mode = mode or _get("CONTEXT_SELECTION_MODE", "hybrid")
    selected = _select_relevant_turns(
        query=rewritten_query or query,
        context=context,
        mode=selection_mode,
    )

    answer_max = _get("CONTEXT_TURN_ANSWER_MAX_CHARS", 150)
    query_max = _get("CONTEXT_TURN_QUERY_MAX_CHARS", 50)
    max_tokens = _get("CONTEXT_MAX_TOKENS", 800)
    recent_required = _get("CONTEXT_RECENT_REQUIRED", 1)

    truncated = [
        _truncate_turn(turn, answer_max, query_max, idx < recent_required)
        for idx, turn in enumerate(selected)
    ]

    return _apply_token_budget(truncated, max_tokens)


def _select_relevant_turns(
    query: str,
    context: List[Dict[str, Any]],
    mode: str,
) -> List[Dict[str, Any]]:
    """按模式筛选相关轮次。context 按时间从旧到新，返回子集（保持顺序）。"""
    if not context:
        return []

    max_turns = _get("CONTEXT_MAX_TURNS", 5)
    recent_required = _get("CONTEXT_RECENT_REQUIRED", 1)

    if mode == "recent_only":
        return context[-max_turns:]

    if mode == "hybrid" or mode == "similarity":
        # hybrid: 最近 recent_required 轮必选，其余按相似度
        recent = context[-recent_required:] if recent_required > 0 else []
        candidate = context[:-recent_required] if recent_required > 0 else context

        if not candidate:
            return recent

        method = _get("CONTEXT_SIMILARITY_METHOD", "keyword")
        scored = []
        for i, turn in enumerate(candidate):
            q = (turn.get("query") or "").strip()
            if not q:
                scored.append((0.0, i, turn))
                continue
            sim = _keyword_similarity(query, q) if method == "keyword" else 0.5
            scored.append((sim, i, turn))

        scored.sort(key=lambda x: (-x[0], -x[1]))
        remain = max(0, max_turns - len(recent))
        selected_turns = [t for _, _, t in scored[:remain]]

        return selected_turns + recent

    return context[-max_turns:]


def _keyword_similarity(query: str, turn_query: str) -> float:
    """
    基于关键词/字符重叠的相似度，0~1。
    使用简单分词（按标点、空格切）+ 集合交集 Jaccard。
    """
    def _tokenize(s: str) -> set:
        s = (s or "").strip()
        s = re.sub(r"[，。？！、；：""''\s]+", " ", s)
        return set(w for w in s.split() if len(w) >= 2)

    a, b = _tokenize(query), _tokenize(turn_query)
    if not a or not b:
        return 0.0
    inter = len(a & b)
    union = len(a | b)
    return inter / union if union > 0 else 0.0


def _truncate_turn(
    turn: Dict[str, Any],
    answer_max: int,
    query_max: int,
    keep_full: bool = False,
) -> Dict[str, Any]:
    """单轮截断。keep_full=True 时 answer 放宽到 2 倍。"""
    q = (turn.get("query") or "").strip()
    a = (turn.get("answer") or "").strip()

    q_lim = query_max
    a_lim = answer_max * 2 if keep_full else answer_max

    if len(q) > q_lim:
        q = q[:q_lim] + "…"
    if len(a) > a_lim:
        a = a[:a_lim] + "…"

    return {"query": q, "answer": a}


def _apply_token_budget(turns: List[Dict[str, Any]], max_tokens: int) -> List[Dict[str, Any]]:
    """
    按 token 预算裁剪。估算：中文约 1 字≈1.5 token，英文 1 词≈1 token。
    从新到旧依次加入，超预算则停止。
    """
    def _est_tokens(s: str) -> int:
        cn = sum(1 for c in s if "\u4e00" <= c <= "\u9fff")
        other = len(s) - cn
        return int(cn * 1.5 + other * 0.4)  # 粗略估算

    result = []
    acc = 0
    for t in reversed(turns):
        q = (t.get("query") or "")
        a = (t.get("answer") or "")
        need = _est_tokens(q) + _est_tokens(a)
        if acc + need > max_tokens and result:
            break
        result.insert(0, t)
        acc += need

    return result
```

### 2.2 导出

在 `app/__init__.py` 中（若存在）可添加导出；若无则无需修改，其他模块直接 `from app.context_compressor import compress_context` 即可。

---

## 三、配置修改：config/settings.py

**修改位置**：在 `# ---------- 增强 RAG 模式 ----------` 块之后、`# ---------- Gradio 演示页 ----------` 之前插入新配置块。

**行号参考**：约第 89 行之后

```python
    # ---------- 增强 RAG 模式 ----------
    USE_LANGCHAIN_RAG: bool = False
    ANSWER_USE_OPINION_FORMAT: bool = True

    # ---------- 对话上下文压缩 ----------   # <-- 新增块开始
    CONTEXT_COMPRESSION_ENABLED: bool = True
    CONTEXT_SELECTION_MODE: str = "hybrid"   # recent_only | similarity | hybrid
    CONTEXT_RECENT_REQUIRED: int = 1
    CONTEXT_MAX_TURNS: int = 5
    CONTEXT_TURN_ANSWER_MAX_CHARS: int = 150
    CONTEXT_TURN_QUERY_MAX_CHARS: int = 50
    CONTEXT_MAX_TOKENS: int = 800
    CONTEXT_SIMILARITY_METHOD: str = "keyword"  # keyword | embedding
    # ---------- 对话上下文压缩 ----------   # <-- 新增块结束

    # ---------- Gradio 演示页 ----------
```

**精确修改方式**：

- 定位到 `ANSWER_USE_OPINION_FORMAT: bool = True` 的下一行
- 在其与 `# ---------- Gradio 演示页 ----------` 之间插入上述 9 行配置

---

## 四、调用点修改

### 4.1 app/chat_service.py

**文件**：`app/chat_service.py`

**修改点**：第 114 行附近，在调用 `generate_answer` 之前对 context 进行压缩。

**原代码**（约第 11-12 行 import、第 113-115 行逻辑）：

```python
# 文件头部 import 区域
from app.answer_engine import generate_answer
from app.compliance import check_and_mask
...
# 第 113-115 行
    # 5. 用检索结果 + 上下文生成答案；这里先不在这层做合规，下面统一做并记日志
    answer = generate_answer(query, ragflow_result, context, do_compliance=False)
```

**修改后**：

```python
# 在 import 区域新增
from app.context_compressor import compress_context

# 第 113-115 行 修改为
    # 5. 用检索结果 + 压缩后上下文生成答案
    compressed = compress_context(
        query,
        context,
        rewritten_query=rewrite_result.get("rewritten_query"),
    )
    answer = generate_answer(query, ragflow_result, compressed, do_compliance=False)
```

**说明**：`context_count` 仍用原始 `context` 长度（第 130 行 `len(context) + 1`），保持与现有一致，无需改动。

---

### 4.2 services/rag/pipeline.py

**文件**：`services/rag/pipeline.py`

**修改点 1**：在 import 区域新增 `compress_context`

**原代码**（约第 29-34 行）：

```python
from app.intent import (
    INTENT_MEDICAL_INSURANCE,
    ...
)
from app.llm_short import extract_insurance_slots
from app.query_rewrite import rewrite as rewrite_query
from app.ragflow_client import call_ragflow
from app.answer_engine import generate_answer
```

**修改后**：在 `from app.answer_engine import generate_answer` 后增加一行：

```python
from app.answer_engine import generate_answer
from app.context_compressor import compress_context
```

**修改点 2**：第 166-169 行，在调用 `generate_answer` 前压缩 context

**原代码**：

```python
    # 用检索到的知识库内容 + 历史 context 生成答案；这里先不做合规，下面统一做并记日志
    dashscope_model = get_dashscope_model_for_plan(model_plan)
    answer = generate_answer(query, ragflow_result, context, do_compliance=False, model=dashscope_model)
```

**修改后**：

```python
    # 用检索到的知识库内容 + 压缩后上下文生成答案
    compressed = compress_context(
        query,
        context,
        rewritten_query=rewrite_result.get("rewritten_query"),
    )
    dashscope_model = get_dashscope_model_for_plan(model_plan)
    answer = generate_answer(query, ragflow_result, compressed, do_compliance=False, model=dashscope_model)
```

---

### 4.3 services/rag/langchain_chain.py

**文件**：`services/rag/langchain_chain.py`

**修改点 1**：import 区域新增 `compress_context`

**原代码**（约第 14-26 行）：

```python
from config import settings
from core.redis_store import get_conversation_context, save_conversation_context
from app.intent import ...
from app.llm_short import extract_insurance_slots
...
from app.answer_engine import (
    INSURANCE_PROMPT_TEMPLATE,
    INSURANCE_OPINION_PROMPT_TEMPLATE,
)
```

**修改后**：在 `from app.answer_engine import` 之后增加：

```python
from app.answer_engine import (
    INSURANCE_PROMPT_TEMPLATE,
    INSURANCE_OPINION_PROMPT_TEMPLATE,
)
from app.context_compressor import compress_context
```

**修改点 2**：第 140-154 行，在构建 `context_str` 之前压缩 context

**原代码**：

```python
    knowledge_content = _build_knowledge_content_from_docs(docs)
    context_str = json.dumps(context, ensure_ascii=False) if context else "无"

    prompt = PromptTemplate(
        input_variables=["knowledge_content", "context", "query"],
        template=_get_prompt_template(),
    )
    model_name = get_dashscope_model_for_plan(model_plan)
    llm = DashScopeLLM(model_name=model_name)
    chain = LLMChain(llm=llm, prompt=prompt)
    answer = chain.run(
        knowledge_content=knowledge_content,
        context=context_str,
        query=query,
    )
```

**修改后**：

```python
    knowledge_content = _build_knowledge_content_from_docs(docs)
    compressed = compress_context(
        query,
        context,
        rewritten_query=rewrite_result.get("rewritten_query"),
    )
    context_str = json.dumps(compressed, ensure_ascii=False) if compressed else "无"

    prompt = PromptTemplate(
        input_variables=["knowledge_content", "context", "query"],
        template=_get_prompt_template(),
    )
    model_name = get_dashscope_model_for_plan(model_plan)
    llm = DashScopeLLM(model_name=model_name)
    chain = LLMChain(llm=llm, prompt=prompt)
    answer = chain.run(
        knowledge_content=knowledge_content,
        context=context_str,
        query=query,
    )
```

---

## 五、可选：Prompt 微调 app/answer_engine.py

**文件**：`app/answer_engine.py`

**修改位置**：`INSURANCE_OPINION_PROMPT_TEMPLATE` 中 `## 历史上下文：` 部分（约第 58-59 行）

**原内容**：

```
## 历史上下文：
{context}

## 用户问题：
```

**可选修改**（帮助模型更好理解压缩后的 context）：

```
## 历史上下文（已按相关性精简）：
{context}

## 用户问题：
```

若产品上希望更明确，可改为：

```
## 历史上下文：
{context}
（以上为与本次问题相关的历史对话，已做精简。请结合其结论与背景回答当前问题，避免重复无关内容。）

## 用户问题：
```

按需选择其一即可。

---

## 六、修改点汇总表

| 序号 | 文件 | 行号（约） | 操作 |
|------|------|------------|------|
| 1 | `app/context_compressor.py` | 新建 | 新增完整模块 |
| 2 | `config/settings.py` | 89-90 之间 | 插入 9 行配置 |
| 3 | `app/chat_service.py` | import + 113-115 | 增加 import，替换 generate_answer 调用 |
| 4 | `services/rag/pipeline.py` | import + 166-169 | 增加 import，在 generate_answer 前压缩 |
| 5 | `services/rag/langchain_chain.py` | import + 140-142 | 增加 import，在 context_str 前压缩 |
| 6 | `app/answer_engine.py` | 58-59 | 可选，Prompt 文案微调 |

---

## 七、依赖说明

- **无新增外部依赖**：`context_compressor` 仅使用 `re`、`typing` 和 `config.settings`
- **jieba 可选**：若需更细粒度分词，可在 `_keyword_similarity` 中接入 jieba，当前实现用正则 + 空格分词即可工作

---

## 八、配置开关

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `CONTEXT_COMPRESSION_ENABLED` | `True` | 设为 `False` 时，`compress_context` 直接返回原 context |
| `CONTEXT_SELECTION_MODE` | `hybrid` | `recent_only`：仅取最近 N 轮；`hybrid`：最近必选 + 相似度筛选 |
| `CONTEXT_RECENT_REQUIRED` | `1` | 最近几轮必定保留 |
| `CONTEXT_MAX_TURNS` | `5` | 筛选后最大轮数 |
| `CONTEXT_TURN_ANSWER_MAX_CHARS` | `150` | 单轮 answer 最大字数 |
| `CONTEXT_TURN_QUERY_MAX_CHARS` | `50` | 单轮 query 最大字数 |
| `CONTEXT_MAX_TOKENS` | `800` | 上下文总 token 预算 |
| `CONTEXT_SIMILARITY_METHOD` | `keyword` | 相似度计算方式 |

---

## 九、验证步骤

1. **关闭压缩**：`CONTEXT_COMPRESSION_ENABLED=false`，行为应与改造前一致。
2. **指代补全**：Q1「蓝医保甲状腺能买吗？」→ Q2「那理赔呢？」，应理解为蓝医保理赔。
3. **Token 控制**：多轮对话后，检查传入 LLM 的 context 总长度是否在预算内。
4. **话题切换**：从产品 A 切到产品 B，回答应聚焦 B，不被 A 过度干扰。

---

*文档版本：v1.0*  
*最后更新：2025-02-25*
