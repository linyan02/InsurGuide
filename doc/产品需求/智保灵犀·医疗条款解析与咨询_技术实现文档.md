# 智保灵犀·医疗条款解析与咨询 —— 技术实现文档

> 对应产品方案：`doc/产品需求/智保灵犀·医疗条款解析与咨询_可执行产品方案.md`

---

## 一、实现概览

| 类型 | 文件 | 操作 |
|------|------|------|
| 新增 | `app/clause_context.py` | 条款上下文管理（Redis 存储、获取、清除） |
| 新增 | `app/ragflow_dataset.py` | RAGflow 知识库创建、文档上传、解析状态轮询 |
| 新增 | `routers/clause.py` | 条款上传、上下文、清除接口 |
| 修改 | `app/intent.py` | 新增 INTENT_CLAUSE_PARSE |
| 修改 | `config/settings.py` | 新增条款相关配置项 |
| 修改 | `core/redis_store.py` | 新增 `get_redis_client()` 供 clause_context 使用 |
| 修改 | `services/rag/pipeline.py` | 意图分支与条款上下文注入 |
| 修改 | `services/rag/pipeline_stream.py` | 流式版同理 |
| 修改 | `app/answer_engine.py` | 条款解析专用 Prompt 模板 |
| 修改 | `api/main.py` | 注册 clause 路由 |
| 修改 | `routers/chat.py` | 支持 clause_text、session_id、clause_loaded |

---

## 二、新增文件：app/clause_context.py

**职责**：条款上下文的 Redis 存储、读取、清除。

```python
"""
医疗条款解析 - 条款上下文管理
"""
from typing import Any, Dict, Optional
import json
import time
from typing import Any, Dict, Optional

from config import settings

# 需在 core/redis_store.py 中新增: def get_redis_client(): return _get_redis()
from core.redis_store import get_redis_client


def _clause_key(user_id: str, session_id: str) -> str:
    sid = session_id or "default"
    return f"clause_ctx:{user_id}:{sid}"


def save_clause_context(
    user_id: str,
    session_id: str,
    source: str,  # "text" | "upload"
    *,
    dataset_id: Optional[str] = None,
    text_preview: Optional[str] = None,
    file_name: Optional[str] = None,
) -> None:
    """保存条款上下文到 Redis。"""
    client = get_redis_client()
    if not client:
        return
    key = _clause_key(user_id, session_id)
    data: Dict[str, Any] = {
        "source": source,
        "created_at": int(time.time()),
    }
    if dataset_id:
        data["dataset_id"] = dataset_id
    if text_preview:
        data["text_preview"] = (text_preview[:500] + "...") if len(text_preview) > 500 else text_preview
    if file_name:
        data["file_name"] = file_name
    ttl = getattr(settings, "REDIS_CONTEXT_TTL_MINUTES", 30) * 60  # 与对话 TTL 一致
    client.setex(key, ttl, json.dumps(data, ensure_ascii=False))


def get_clause_context(user_id: str, session_id: str) -> Optional[Dict[str, Any]]:
    """获取当前会话的条款上下文。"""
    client = get_redis_client()
    if not client:
        return None
    key = _clause_key(user_id, session_id)
    raw = client.get(key)
    if not raw:
        return None
    try:
        return json.loads(raw)
    except Exception:
        return None


def clear_clause_context(user_id: str, session_id: str) -> bool:
    """清除条款上下文。"""
    client = get_redis_client()
    if not client:
        return False
    key = _clause_key(user_id, session_id)
    client.delete(key)
    return True
```

---

## 三、新增文件：app/ragflow_dataset.py

**职责**：调用 RAGflow API 创建用户条款知识库、上传文档、轮询解析状态。

### 3.1 RAGflow API 基础 URL

```python
# 配置 RAGFLOW_API_URL 通常为 http://host:9380/api/v1
# 创建 Dataset: POST {base}/datasets
# 上传文档: POST {base}/datasets/{dataset_id}/documents  (multipart/form-data)
# 解析文档: POST {base}/datasets/{dataset_id}/chunks  (document_ids)
# 列举文档: GET {base}/datasets/{dataset_id}/documents
```

### 3.2 实现接口

```python
"""
RAGflow 知识库与文档管理 - 用于用户条款上传
"""
from typing import Any, Dict, List, Optional
import httpx
import time
from config import settings


def _base_url() -> str:
    url = (settings.RAGFLOW_API_URL or "").rstrip("/")
    if "/retrieval" in url:
        url = url.replace("/retrieval", "")
    if not url.endswith("/api/v1"):
        url = url.rstrip("/") + "/api/v1" if url else ""
    return url


def _headers() -> Dict[str, str]:
    headers = {"Content-Type": "application/json"}
    key = getattr(settings, "RAGFLOW_API_KEY", None)
    if key:
        headers["Authorization"] = f"Bearer {key}"
    return headers


def create_clause_dataset(name: str) -> Dict[str, Any]:
    """
    创建用户条款专用知识库。
    chunk_method 使用 "laws" 或 "naive"，适合条款类文档。
    """
    url = _base_url() + "/datasets"
    chunk_method = getattr(settings, "CLAUSE_KB_CHUNK_METHOD", "laws")
    payload = {
        "name": name,
        "chunk_method": chunk_method if chunk_method in ("laws", "naive", "book") else "naive",
    }
    with httpx.Client(timeout=30) as client:
        resp = client.post(url, json=payload, headers=_headers())
    if resp.status_code != 200:
        return {"error": f"创建知识库失败: HTTP {resp.status_code}"}
    data = resp.json()
    if data.get("code") != 0:
        return {"error": data.get("message", "创建知识库失败")}
    return {"dataset_id": data.get("data", {}).get("id"), "name": name}


def upload_document(dataset_id: str, file_content: bytes, file_name: str) -> Dict[str, Any]:
    """
    上传文档到指定知识库。
    file_content: 文件二进制内容
    file_name: 文件名（含扩展名）
    """
    url = _base_url() + f"/datasets/{dataset_id}/documents"
    headers = {}
    key = getattr(settings, "RAGFLOW_API_KEY", None)
    if key:
        headers["Authorization"] = f"Bearer {key}"
    files = {"file": (file_name, file_content)}
    with httpx.Client(timeout=60) as client:
        resp = client.post(url, files=files, headers=headers)
    if resp.status_code != 200:
        return {"error": f"上传失败: HTTP {resp.status_code}"}
    data = resp.json()
    if data.get("code") != 0:
        return {"error": data.get("message", "上传失败")}
    doc_list = data.get("data") or []
    doc_ids = [d.get("id") for d in doc_list if isinstance(d, dict) and d.get("id")]
    return {"document_ids": doc_ids, "dataset_id": dataset_id}


def parse_documents(dataset_id: str, document_ids: List[str]) -> Dict[str, Any]:
    """触发文档解析。"""
    url = _base_url() + f"/datasets/{dataset_id}/chunks"
    payload = {"document_ids": document_ids}
    with httpx.Client(timeout=30) as client:
        resp = client.post(url, json=payload, headers=_headers())
    if resp.status_code != 200:
        return {"error": f"解析请求失败: HTTP {resp.status_code}"}
    data = resp.json()
    if data.get("code") != 0:
        return {"error": data.get("message", "解析请求失败")}
    return {"ok": True}


def wait_for_parsing(dataset_id: str, document_ids: List[str], max_wait: int = 120) -> bool:
    """
    轮询文档解析状态，直到完成或超时。
    通过 GET /datasets/{id}/documents 检查 run 状态。
    """
    url = _base_url() + f"/datasets/{dataset_id}/documents"
    headers = _headers()
    start = time.time()
    while time.time() - start < max_wait:
        with httpx.Client(timeout=10) as client:
            resp = client.get(url + "?page=1&page_size=100", headers=headers)
        if resp.status_code != 200:
            time.sleep(3)
            continue
        data = resp.json()
        if data.get("code") != 0:
            time.sleep(3)
            continue
        docs = data.get("data") or []
        all_done = True
        for doc in docs:
            if not isinstance(doc, dict):
                continue
            if doc.get("id") not in document_ids:
                continue
            run = doc.get("run", 0)
            if run in (0, 1):  # UNSTART, RUNNING
                all_done = False
                break
            if run == 4:  # FAIL
                return False
        if all_done:
            return True
        time.sleep(5)
    return False
```

---

## 四、新增路由：routers/clause.py

```python
"""
医疗条款解析 - 上传、上下文、清除接口
"""
from fastapi import APIRouter, Body, Depends, File, HTTPException, UploadFile
from pydantic import BaseModel
from typing import Optional
from core.auth import get_current_active_user
from models.user import User
from app.clause_context import save_clause_context, get_clause_context, clear_clause_context
from app.ragflow_dataset import (
    create_clause_dataset,
    upload_document,
    parse_documents,
    wait_for_parsing,
)
from config import settings

router = APIRouter(prefix="/api/clause", tags=["医疗条款解析"])


# ---------- 配置 ----------
def _allowed_ext() -> set:
    ext = getattr(settings, "CLAUSE_ALLOWED_EXT", "pdf,doc,docx,txt")
    return {x.strip().lower() for x in ext.split(",") if x.strip()}


def _max_size() -> int:
    return getattr(settings, "CLAUSE_UPLOAD_MAX_SIZE", 10 * 1024 * 1024)


# ---------- 请求模型 ----------
class ClearClauseBody(BaseModel):
    session_id: Optional[str] = None


# ---------- 接口 ----------
@router.post("/upload")
async def clause_upload(
    file: UploadFile = File(...),
    session_id: Optional[str] = None,
    current_user: User = Depends(get_current_active_user),
):
    """上传医疗条款文件（PDF/Word/TXT）。"""
    if not getattr(settings, "CLAUSE_PARSE_ENABLED", True):
        raise HTTPException(status_code=503, detail="条款解析功能暂未开放")

    ext = (file.filename or "").split(".")[-1].lower()
    if ext not in _allowed_ext():
        raise HTTPException(400, detail=f"不支持的文件格式，仅支持: {', '.join(_allowed_ext())}")

    content = await file.read()
    if len(content) > _max_size():
        raise HTTPException(400, detail="文件大小超出限制")

    user_id = current_user.username

    # 每次上传创建新知识库（name 加时间戳避免重名）
    import time
    ds_name = f"clause_{user_id}_{(session_id or 'default')}_{int(time.time())}"
    create_res = create_clause_dataset(ds_name)
    if "error" in create_res:
        raise HTTPException(500, detail=create_res["error"])
    dataset_id = create_res.get("dataset_id")
    if not dataset_id:
        raise HTTPException(500, detail="创建知识库失败")

    upload_res = upload_document(dataset_id, content, file.filename or "upload.pdf")
    if "error" in upload_res:
        raise HTTPException(500, detail=upload_res["error"])

    doc_ids = upload_res.get("document_ids") or []
    if not doc_ids:
        raise HTTPException(500, detail="上传成功但未返回文档 ID")

    parse_documents(dataset_id, doc_ids)

    if not wait_for_parsing(dataset_id, doc_ids):
        raise HTTPException(504, detail="文件解析超时，请稍后重试或粘贴文本")

    save_clause_context(
        user_id, session_id or "default",
        source="upload",
        dataset_id=dataset_id,
        file_name=file.filename,
    )

    return {
        "message": "条款已加载，可以开始提问",
        "dataset_id": dataset_id,
        "file_name": file.filename,
    }


@router.get("/context")
def clause_context(
    session_id: Optional[str] = None,
    current_user: User = Depends(get_current_active_user),
):
    """获取当前会话的条款上下文。"""
    ctx = get_clause_context(current_user.username, session_id or "default")
    if not ctx:
        return {"loaded": False, "context": None}
    return {"loaded": True, "context": ctx}


@router.post("/clear")
def clause_clear(
    body: Optional[ClearClauseBody] = Body(None),
    current_user: User = Depends(get_current_active_user),
):
    """清除当前会话的条款上下文。"""
    sid = (body.session_id if body else None) or "default"
    clear_clause_context(current_user.username, sid)
    return {"message": "已清除条款上下文"}
```

---

## 五、修改 app/intent.py

### 5.1 新增意图常量

```python
INTENT_CLAUSE_PARSE = "clause_parse"

ALL_INTENTS = [
    ...
    INTENT_CLAUSE_PARSE,
    INTENT_OTHER,
]

INTENT_LABELS_CN = {
    ...
    INTENT_CLAUSE_PARSE: "医疗条款解析",
    INTENT_OTHER: "其他",
}

INTENT_KEYWORDS[INTENT_CLAUSE_PARSE] = [
    "条款", "合同", "保险合同", "保险责任", "责任免除",
    "免赔额", "赔付比例", "等待期", "续保条件",
    "这段什么意思", "这条怎么理解", "能赔吗", "能不能报",
    "上传条款", "解析条款", "解读条款", "条款咨询",
]
```

### 5.2 识别逻辑补充

在 `recognize_rule` 中，若 `INTENT_CLAUSE_PARSE` 关键词命中，且上下文存在 `clause_ctx` 或本次请求带 `clause_text`，可适当提高该意图的置信度。

---

## 六、修改 config/settings.py

```python
# ---------- 医疗条款解析与咨询 ----------
CLAUSE_PARSE_ENABLED: bool = True
CLAUSE_UPLOAD_MAX_SIZE: int = 10 * 1024 * 1024   # 10MB
CLAUSE_ALLOWED_EXT: str = "pdf,doc,docx,txt"
CLAUSE_KB_CHUNK_METHOD: str = "laws"             # naive | laws | book
```

---

## 七、修改 services/rag/pipeline.py

### 7.1 分支逻辑

在 `run_chat_pipeline` 中，意图识别后：

```python
from app.intent import INTENT_CLAUSE_PARSE
from app.clause_context import get_clause_context

# 在意图识别之后
if intent_name == INTENT_CLAUSE_PARSE and getattr(settings, "CLAUSE_PARSE_ENABLED", True):
    clause_ctx = get_clause_context(user_id, session_id)
    clause_text = (body_dict or {}).get("clause_text")  # 若请求体带 clause_text

    # 若既无上下文也无本次 clause_text，可返回引导语
    if not clause_ctx and not clause_text:
        return {
            "answer": "要准确解读条款，请先粘贴相关条款内容，或上传条款文件（PDF/Word/TXT）。",
            "sources": [],
            "intent": intent_name,
            "intent_cn": get_intent_label_cn(intent_name),
            "state": "GUIDING",
        }

    # 构建检索用的 dataset_ids：用户条款库优先，叠加默认知识库
    if clause_text:
        # 文本模式：将 clause_text 注入 prompt，或先写入临时 Chroma 再检索
        # 一期简化：直接拼接到 knowledge_content 前
        pass  # 见下方 answer_engine
```

### 7.2 检索调用

```python
# 条款解析时：用户条款库 + 默认知识库
default_kb = settings.RAGFLOW_KNOWLEDGE_BASE_ID
if clause_ctx and clause_ctx.get("dataset_id"):
    kb_ids = [clause_ctx["dataset_id"]]
    if default_kb and default_kb not in kb_ids:
        kb_ids.append(default_kb)
else:
    kb_ids = [default_kb] if default_kb else []

rag_result = call_ragflow(
    query=rewritten_query,
    knowledge_base_id=kb_ids if kb_ids else None,
    top_k=settings.RAGFLOW_TOP_K,
)
```

### 7.3 传递 clause_text 到答案生成

若请求带 `clause_text`，在调用 `generate_answer` 时传入：

```python
generate_answer(
    ...,
    intent_name=intent_name,
    clause_text=clause_text,
    clause_context=clause_ctx,
)
```

---

## 八、修改 app/answer_engine.py

### 8.1 新增 Prompt 模板

```python
CLAUSE_PARSE_PROMPT_TEMPLATE = """你是专业的保险条款解读顾问。请基于以下知识库内容与用户提供的条款，回答用户问题。

## 输出要求（必须 Markdown）
1. **## 智保灵犀·条款解读**
2. **您的问题：** [简要复述]
3. **条款依据：** 若知识库中有相关原文，请用引用块标注
4. **解读：** 结构化、易懂的解读
5. 结尾附：本解读仅供参考，不构成法律或投保建议，具体以保险合同条款为准。

## 知识库内容
{knowledge_content}

## 用户提供的条款（如有）
{clause_text}

## 用户问题
{query}
"""
```

### 8.2 分支逻辑

在 `generate_answer` 中，若 `intent_name == INTENT_CLAUSE_PARSE`：

```python
if intent_name == "clause_parse":
    clause_text = kwargs.get("clause_text") or ""
    prompt = CLAUSE_PARSE_PROMPT_TEMPLATE.format(
        knowledge_content=knowledge_content,
        clause_text=clause_text or "（未提供，仅依据知识库）",
        query=query,
    )
```

---

## 九、修改 routers/chat.py

### 9.1 请求体扩展

```python
class ChatRequest(BaseModel):
    user_id: str
    query: str
    # ... 现有字段 ...
    clause_text: Optional[str] = None   # 用户粘贴的条款文本
    session_id: Optional[str] = None  # 会话 ID，用于取 clause_ctx
```

### 9.2 传递 clause_text 与 session_id

在调用 `run_chat_pipeline` 时传入 `clause_text`、`session_id`，pipeline 内部用 `session_id` 取 `clause_ctx`。

### 9.3 响应扩展

```python
# 若存在条款上下文，响应中可增加
"clause_loaded": True/False
```

---

## 十、修改 api/main.py

```python
from routers import clause
app.include_router(clause.router)
```

---

## 十一、前端改动建议（web/static/index.html）

1. **上传入口**：在输入框上方增加「上传条款」按钮，调用 `POST /api/clause/upload`（需鉴权）
2. **条款状态**：上传成功后显示「已加载：xxx.pdf」，提供「清除」调用 `POST /api/clause/clear`
3. **文本模式**：可选提供「粘贴条款」折叠区，用户粘贴后与问题一起发送，`clause_text` 作为请求体字段

---

## 十二、调用链路示意

```
用户上传 PDF
  ↓
POST /api/clause/upload → create_clause_dataset → upload_document → parse → wait
  ↓
save_clause_context(dataset_id) → Redis
  ↓
用户: "免赔额怎么算？"
  ↓
POST /api/chat { user_id, query, session_id }
  ↓
意图识别 → INTENT_CLAUSE_PARSE
  ↓
get_clause_context → dataset_id
  ↓
call_ragflow(dataset_ids=[用户库, 默认库])
  ↓
generate_answer(..., template=CLAUSE_PARSE_PROMPT_TEMPLATE)
  ↓
合规校验 → 返回
```

---

## 十三、修改 core/redis_store.py

新增公共方法供 clause_context 使用：

```python
def get_redis_client():
    """获取 Redis 客户端，供其他模块复用。"""
    return _get_redis()
```

---

## 十四、Redis 键约定

| Key | 说明 | TTL |
|-----|------|-----|
| `clause_ctx:{user_id}:{session_id}` | 条款上下文 JSON | 与对话一致（REDIS_CONTEXT_TTL_MINUTES） |

---

## 十五、依赖与权限

- **RAGflow**：需支持 Dataset 创建、文档上传、解析 API（标准 RAGflow 均支持）
- **认证**：`/api/clause/*` 建议使用 `get_current_active_user` 鉴权
- **匿名用户**：若支持匿名对话，可改用 `user_id` 作为会话标识，暂不强制登录

---

*文档版本：v1.0*  
*最后更新：2025-02-26*
