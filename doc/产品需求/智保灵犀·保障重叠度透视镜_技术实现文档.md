# 智保灵犀·保障重叠度透视镜 —— 技术实现文档

> 对应产品方案：`doc/产品需求/智保灵犀·保障重叠度透视镜_可执行产品方案.md`

---

## 一、实现概览

| 类型 | 文件 | 操作 |
|------|------|------|
| 新增 | `app/coverage_schemas.py` | Slot Schema 定义 |
| 新增 | `app/coverage_slots.py` | Slot 提取与反问 |
| 新增 | `app/coverage_overlap.py` | 重叠度计算逻辑 |
| 修改 | `app/intent.py` | 新增 INTENT_COVERAGE_OVERLAP |
| 修改 | `config/settings.py` | 新增配置项（含多库、query 增强） |
| 修改 | `app/ragflow_client.py` | 支持多库、keyword、query 增强透传 |
| 修改 | `services/rag/pipeline.py` | 意图分支与调用 |
| 修改 | `app/answer_engine.py` | 该意图的生成逻辑 |

---

## 二、新增文件：app/coverage_schemas.py

**职责**：定义保障重叠度分析所需的 Slot 结构，供 `coverage_slots` 与 `coverage_overlap` 使用。

```python
"""
保障重叠度 Slot Schema - 现有保障与待购险种的结构化定义
"""
from typing import Any, Dict, List, Optional

INTENT_COVERAGE_OVERLAP = "coverage_overlap"

COVERAGE_OVERLAP_SCHEMA: Dict[str, Any] = {
    "description": "保障重叠度分析（多险种通用，见产品方案 §3.5）",
    "required_fields": [
        "existing_coverage_list",
        "pending_insurance",
    ],
    "field_specs": {
        "existing_coverage_list": {
            "type": "list",
            "description": "用户现有保障列表，属性按险种类别选用",
            "item_spec": {
                "name": "险种名称（社保/百万医疗/重疾险/学平险/意外险/防癌险等）",
                "deductible": "免赔额（医疗险）",
                "has_outpatient": "是否含门诊（医疗险）",
                "coverage_scope": "保障范围简述（可选）",
                "death_benefit": "身故保额（意外/寿险类，可选）",
                "critical_illness_amount": "重疾保额（重疾险，可选）",
            },
        },
        "pending_insurance": {
            "type": "str",
            "description": "待购险种名称",
        },
    },
}
```

---

## 三、新增文件：app/coverage_slots.py

**职责**：从用户问题与上下文中提取保障 Slot，未补全时生成追问语。

### 3.1 接口设计

```python
def extract_coverage_slots(
    query: str,
    context: List[Dict[str, Any]],
) -> Dict[str, Any]:
    """
    提取保障重叠度分析所需的 Slot。
    返回：
    {
        "is_complete": bool,
        "slots": { "existing_coverage_list": [...], "pending_insurance": "..." },
        "guide_question": str | None,  # 未补全时的追问
    }
    """
```

### 3.2 Prompt 设计（参考 extract_insurance_slots）

```python
EXTRACT_COVERAGE_SLOTS_PROMPT = """你是一位保障分析顾问。用户想判断「待购险种」是否与「现有保障」重复。

## 任务
从用户问题与历史对话中提取：
1. existing_coverage_list：已有保障，每项需含 name、deductible、has_outpatient
2. pending_insurance：待购险种名

## 险种名标准化（可扩展，见产品方案 §3.5 险种矩阵）
- 社保/医保/城镇居民医保 → 社保
- 商业补充医疗/公司团险/补充医疗 → 商业补充医疗
- 百万医疗/医疗险(大额) → 百万医疗
- 学平险/学生险 → 学平险
- 意外险/意外伤害险 → 意外险
- 重疾险/重大疾病保险 → 重疾险
- 防癌险/防癌医疗险 → 按给付/报销区分

## 关键变量
- deductible：免赔额，0 或数字（单位元）
- has_outpatient：是否含门诊，true/false

## 输出规则
- 若 existing_coverage_list 或 pending_insurance 缺失 → is_complete=false
- 若「商业补充医疗」存在但 deductible、has_outpatient 任一缺失 → is_complete=false，guide_question 追问
- 追问语气亲切，一次只问 1-2 个关键点

## 历史上下文
{context}

## 用户问题
{query}

## 输出格式（严格 JSON）
{
  "is_complete": true/false,
  "existing_coverage_list": [{"name":"社保","deductible":null,"has_outpatient":true}, ...],
  "pending_insurance": "学平险",
  "guide_question": null 或 "追问内容"
}
"""
```

### 3.3 实现要点

- 调用 `app.llm_short.call()` 执行 LLM 请求
- 解析 JSON，处理异常时返回 `is_complete=false`，`guide_question="抱歉，我没完全理解您的保障情况，您能简单列举一下已有的保险吗？"`

---

## 四、新增文件：app/coverage_overlap.py

**职责**：基于 Slot 与 RAG 检索结果，计算保障区间重叠，产出结构化分析结果供 LLM 生成最终答案。

### 4.1 一期：规则实现

```python
def compute_coverage_gap(
    slots: Dict[str, Any],
    rag_docs: List[str],
) -> Dict[str, Any]:
    """
    计算保障重叠情况（简化规则版）。
    返回：
    {
        "overlap_matrix": [  # 风险场景 vs 重叠判定
            {"scene": "小病/门诊", "existing": "...", "pending": "...", "overlap": "重叠/部分/无"},
            ...
        ],
        "marginal_contribution": "高/中/低",  # 待购险种边际贡献
        "recommendation": "买/不买/换",       # 建议
    }
    """
```

**规则逻辑（示例）**：

- 若存在「商业补充医疗」且 deductible=0、has_outpatient=true → 小病/门诊区间已覆盖
- 若存在「百万医疗」→ 大病住院(>1万)已覆盖
- 学平险的「身故/伤残」→ 多数医疗险不覆盖，标为「唯一差异」
- 若医疗责任重叠高且身故额低 → recommendation="不买"或"换购意外险"

---

## 五、修改 app/intent.py

### 5.1 新增意图常量

```python
INTENT_COVERAGE_OVERLAP = "coverage_overlap"
ALL_INTENTS = [..., INTENT_COVERAGE_OVERLAP]

# 中文标签
INTENT_LABELS_CN = {
    ...
    INTENT_COVERAGE_OVERLAP: "保障重叠分析",
}
```

### 5.2 关键词扩展（rule 模式）

```python
INTENT_KEYWORDS[INTENT_COVERAGE_OVERLAP] = [
    "重叠", "重复", "浪费", "冗余", "重复建设",
    "还需要买吗", "要不要买", "值不值得买",
    "学平险浪费", "经纪人说浪费", "代理人说",
    "已有社保", "有百万医疗", "有补充医疗",
]
```

---

## 六、修改 config/settings.py

```python
# ---------- 保障重叠度透视镜 ----------
COVERAGE_OVERLAP_ENABLED: bool = True
COVERAGE_OVERLAP_KB_ID: Optional[str] = None      # 单库时用
COVERAGE_OVERLAP_KB_IDS: Optional[str] = None     # 多库：逗号分隔 "id1,id2"，空则用默认库
COVERAGE_OVERLAP_TOP_K: int = 5
COVERAGE_OVERLAP_QUERY_ENHANCE: bool = True       # 是否启用 query 增强
COVERAGE_OVERLAP_KEYWORD: bool = True             # RAGflow 是否启用 keyword 匹配
```

---

## 七、修改 services/rag/pipeline.py

在 `INTENT_MEDICAL_INSURANCE` 分支之后增加：

```python
from app.coverage_slots import extract_coverage_slots
from app.intent import INTENT_COVERAGE_OVERLAP

# 在 run_chat_pipeline 中，意图识别后：
if intent_name == INTENT_COVERAGE_OVERLAP and getattr(settings, "COVERAGE_OVERLAP_ENABLED", True):
    slots_result = extract_coverage_slots(query, context)
    if not slots_result.get("is_complete", False):
        guide_question = slots_result.get("guide_question") or "请补充您的保障情况以便分析。"
        return {
            "answer": guide_question,
            "sources": [],
            "intent": intent_name,
            "intent_cn": get_intent_label_cn(intent_name),
            "state": "GUIDING",
        }
    # 补全后：RAG 检索 + coverage_overlap 计算 + 专用生成
    # ...
```

---

## 八、修改 app/answer_engine.py

### 8.1 新增 Prompt 模板

```python
COVERAGE_OVERLAP_PROMPT_TEMPLATE = """你是保障分析专家。基于检索到的知识库内容，对用户的保障重叠问题给出结构化分析。

## 输出要求（必须 Markdown）
1. **## 智保灵犀分析结果**
2. **结论：** 一句话（支持/反驳经纪人建议 + 简要理由）
3. **## 保障重叠透视表**
   | 风险场景 | 您的现有防线 | {pending_insurance}的作用 | 重叠判定 |
   | --- | --- | --- | --- |
   [根据 analysis_result 填充]
4. **## 专家建议** [买/不买/换个买法] + 理由
5. 合规声明：本内容仅供参考，不构成投保建议，具体以保险合同条款为准。

## 知识库内容
{knowledge_content}

## 预分析结果（已计算）
{analysis_result}

## 用户现有保障
{existing_coverage}

## 待购险种
{pending_insurance}

## 用户问题
{query}
"""
```

### 8.2 分支逻辑

在 `generate_answer` 或调用处，根据 `intent` 或额外参数 `intent_name` 判断：若为 `coverage_overlap`，则使用上述模板，并传入 `analysis_result`（来自 `compute_coverage_gap`）。

---

## 九、RAGflow 多库与 Query 增强实现

### 9.1 多库支持

`call_ragflow` 已支持 `knowledge_base_id` 为列表，直接透传给 `dataset_ids`：

```python
# app/ragflow_client.py 现有逻辑（无需改）
if kb_id:
    payload["dataset_ids"] = [kb_id] if isinstance(kb_id, str) else list(kb_id)
```

调用方传入列表即可：

```python
kb_ids = _get_coverage_kb_ids()  # 返回 ["id1", "id2"] 或 [default_id]
call_ragflow(query=..., knowledge_base_id=kb_ids, ...)
```

解析配置：

```python
def _get_coverage_kb_ids() -> List[str]:
    ids_str = getattr(settings, "RAGFLOW_COVERAGE_KB_IDS", None) or getattr(settings, "COVERAGE_OVERLAP_KB_ID", None)
    default = settings.RAGFLOW_KNOWLEDGE_BASE_ID
    if not ids_str:
        return [default] if default else []
    return [x.strip() for x in str(ids_str).split(",") if x.strip()] or [default]
```

### 9.2 Query 增强

```python
# app/query_enhance.py 或 pipeline 内联
def enhance_query_for_intent(query: str, intent: str) -> str:
    if intent == "coverage_overlap" and getattr(settings, "COVERAGE_OVERLAP_QUERY_ENHANCE", True):
        return f"{query} 免赔额 门诊 住院 身故 险种责任 理赔逻辑"
    return query
```

### 9.3 keyword 参数透传

若需启用 RAGflow 的 keyword 匹配，在 `call_ragflow` 增加可选参数并写入 payload：

```python
def call_ragflow(
    query: str,
    knowledge_base_id: Optional[Union[str, List[str]]] = None,
    top_k: Optional[int] = None,
    keyword: Optional[bool] = None,  # 新增
    ...
) -> Dict[str, Any]:
    ...
    if keyword is not None:
        payload["keyword"] = keyword
```

---

## 十、调用链路示意

```
用户: "我有社保、补充医疗、百万医疗，学平险浪费吗？"
  ↓
意图识别 → INTENT_COVERAGE_OVERLAP
  ↓
extract_coverage_slots → 缺「补充医疗免赔额、门诊」
  ↓
返回 guide_question: "您的商业补充医疗免赔额是多少？是否含门诊？"
  ↓
用户: "0免赔，含门诊"
  ↓
extract_coverage_slots → is_complete=true
  ↓
Query 增强（追加：免赔额 门诊 身故 险种责任...）
  ↓
RAGflow 检索（多库 dataset_ids=[KB1,KB2]、keyword=true）
  ↓
compute_coverage_gap(slots, rag_docs)
  ↓
generate_answer(..., template=COVERAGE_OVERLAP_PROMPT_TEMPLATE)
  ↓
合规校验 → 返回
```

---

*文档版本：v1.0*  
*最后更新：2025-02-25*
