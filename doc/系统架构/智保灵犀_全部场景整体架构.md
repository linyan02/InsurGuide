# 智保灵犀（InsurGuide）—— 全部场景整体架构

> 基于整个项目的一体化架构视图，覆盖通用咨询、百万医疗推荐、保障重叠度透视镜、医疗条款解析等所有业务场景  
> 文档版本：v1.0 | 生成日期：2026-02-28

---

## 一、架构总览

智保灵犀是基于 FastAPI、LangChain、RAGflow 的智能保险指南与增强 RAG 系统，采用**分层架构**，支持多轮对话、意图识别、问题改写、知识库检索、答案生成与合规校验，并针对不同业务场景（百万医疗、保障重叠、条款解析）提供专用流程。

---

## 二、全部场景整体架构图

```mermaid
flowchart TB
    subgraph 用户与终端["用户与终端"]
        U[👤 用户]
        PC[PC Web<br/>/static/index.html]
        ADMIN[管理端<br/>/static/admin.html]
        G[Gradio 演示<br/>gradio_ui]
    end

    subgraph API层["API 接口层 (api/ + routers/)"]
        FASTAPI[FastAPI 应用]
        R_AUTH["/api/auth<br/>注册/登录/me"]
        R_CHAT["/api/chat<br/>对话/流式/清除/历史"]
        R_CLAUSE["/api/clause<br/>条款上传/上下文/清除"]
        R_VECTOR["/api/vector<br/>向量库管理"]
        R_ES["/api/es<br/>Elasticsearch"]
        R_RULES["/api/intent-rules<br/>意图/改写规则"]
        R_ADMIN["/api/admin<br/>管理看板"]
    end

    subgraph 增强RAG层["增强 RAG 服务层 (services/rag/ + app/)"]
        PIPE[流水线编排<br/>pipeline / pipeline_stream]
        INTENT[意图识别<br/>intent.py]
        REWRITE[问题改写<br/>query_rewrite.py]
        RAGFLOW[RAGflow 检索<br/>ragflow_client]
        ANS[答案生成<br/>answer_engine]
        COMP[合规校验<br/>compliance]
        direction TB
    end

    subgraph 业务场景分支["意图分支 → 专用流程"]
        B1[通用咨询<br/>retrieval/consultation等]
        B2[百万医疗<br/>INTENT_MEDICAL_INSURANCE]
        B3[保障重叠<br/>INTENT_COVERAGE_OVERLAP]
        B4[条款解析<br/>INTENT_CLAUSE_PARSE]
    end

    subgraph 专用模块["场景专用模块"]
        SLOTS[要素提取<br/>extract_insurance_slots<br/>extract_coverage_slots]
        COVERAGE[重叠计算<br/>compute_coverage_gap]
        CLAUSE_CTX[条款上下文<br/>clause_context]
        CMPRS[上下文压缩<br/>context_compressor]
    end

    subgraph 基础设施["核心基础设施层 (core/)"]
        MYSQL[(MySQL<br/>用户/日志)]
        REDIS[(Redis<br/>对话上下文/条款上下文)]
        CHROMA[(ChromaDB<br/>意图/改写规则向量)]
        ES[(Elasticsearch<br/>可选)]
        JWT[JWT 认证]
    end

    subgraph 外部服务["外部服务"]
        RAGFLOW_SVC[RAGflow<br/>知识库]
        LLM[通义千问<br/>DashScope API]
    end

    U --> PC & ADMIN & G
    PC --> FASTAPI
    ADMIN --> FASTAPI
    G --> FASTAPI
    FASTAPI --> R_AUTH & R_CHAT & R_CLAUSE & R_VECTOR & R_ES & R_RULES & R_ADMIN
    R_CHAT --> PIPE
    R_CLAUSE --> CLAUSE_CTX
    R_ADMIN --> MYSQL
    PIPE --> INTENT --> B1 & B2 & B3 & B4
    PIPE --> REDIS
    B2 --> SLOTS
    B3 --> SLOTS --> COVERAGE
    B4 --> CLAUSE_CTX
    B1 & B2 & B3 & B4 --> REWRITE
    REWRITE --> CMPRS
    CMPRS --> RAGFLOW
    RAGFLOW --> ANS
    ANS --> COMP
    COMP --> REDIS & MYSQL
    INTENT --> CHROMA
    REWRITE --> CHROMA
    RAGFLOW --> RAGFLOW_SVC
    ANS --> LLM
    R_AUTH --> JWT
```

---

## 三、全场景意图分支与数据流

```mermaid
flowchart TB
    subgraph 入口["请求入口"]
        REQ[用户提问 + 可选 clause_text/session_id]
    end

    subgraph 上下文["上下文获取"]
        REDIS_CTX[Redis: 对话历史]
        CLAUSE_CTX[Redis: 条款上下文<br/>clause_ctx:user:session]
    end

    subgraph 意图识别["意图识别 (4 模式)"]
        I_RULE[rule 关键词]
        I_LLM[llm 大模型]
        I_LLM_VEC[llm_vector 向量+LLM]
        I_BERT[bert 自训练]
    end

    subgraph 意图类型["意图类型"]
        T1[retrieval 条款检索]
        T2[consultation 一般咨询]
        T3[claims 理赔]
        T4[underwriting 核保]
        T5[product 产品咨询]
        T6[medical_insurance<br/>百万医疗]
        T7[coverage_overlap<br/>保障重叠]
        T8[clause_parse<br/>条款解析]
        T9[greeting 问候]
    end

    subgraph 场景流程["各场景主流程"]
        S1[通用: 改写 → RAGflow → 生成]
        S2[百万医疗: Slot 提取 → 反问/补全 → RAG 推荐]
        S3[保障重叠: 双 Slot 提取 → 差集计算 → 表格建议]
        S4[条款解析: 条款入库 → 双库检索 → 条款+知识库回答]
    end

    subgraph 输出["输出与落库"]
        OUT[答案 + 溯源 + 富文本]
        LOG[MySQL 交互/合规日志]
        SAVE_CTX[Redis 保存上下文]
    end

    REQ --> REDIS_CTX & CLAUSE_CTX
    REDIS_CTX --> I_RULE & I_LLM & I_LLM_VEC & I_BERT
    CLAUSE_CTX --> I_RULE
    I_RULE & I_LLM & I_LLM_VEC & I_BERT --> T1 & T2 & T3 & T4 & T5 & T6 & T7 & T8 & T9
    T1 & T2 & T3 & T4 & T5 & T9 --> S1
    T6 --> S2
    T7 --> S3
    T8 --> S4
    S1 & S2 & S3 & S4 --> OUT --> LOG & SAVE_CTX
```

---

## 四、增强 RAG 流水线（全场景统一编排）

```mermaid
sequenceDiagram
    participant Client as 客户端
    participant API as routers/chat
    participant Pipe as pipeline
    participant Redis
    participant Intent as intent
    participant Slots as 要素/Slot
    participant Overlap as 重叠计算
    participant ClauseCtx as 条款上下文
    participant Rewrite as query_rewrite
    participant RAGflow
    participant Answer as answer_engine
    participant Comp as compliance
    participant MySQL

    Client->>API: POST /api/chat {user_id, query, clause_text?, session_id?}
    API->>Pipe: run_chat_pipeline()
    Pipe->>Redis: get_conversation_context
    Redis-->>Pipe: context[]

    alt 条款解析场景: clause_text 或 clause_ctx 存在
        Pipe->>ClauseCtx: get_clause_context / 上传入库
        ClauseCtx-->>Pipe: 用户条款知识库 ID
    end

    Pipe->>Intent: recognize(query)
    Intent-->>Pipe: intent, confidence

    alt INTENT_MEDICAL_INSURANCE
        Pipe->>Slots: extract_insurance_slots
        Slots-->>Pipe: slots / GUIDING 反问
    else INTENT_COVERAGE_OVERLAP
        Pipe->>Slots: extract_coverage_slots
        Pipe->>Overlap: compute_coverage_gap
        Overlap-->>Pipe: 重叠表 + 建议
    else INTENT_CLAUSE_PARSE
        Pipe->>ClauseCtx: 确保条款已加载
    end

    Pipe->>Rewrite: rewrite(query, context)
    Rewrite-->>Pipe: rewritten_query
    Pipe->>Pipe: compress_context (上下文压缩)
    Pipe->>RAGflow: call_ragflow (单库或 用户条款+平台库)
    RAGflow-->>Pipe: documents
    Pipe->>Answer: generate_answer (按意图选模板)
    Answer-->>Pipe: answer
    Pipe->>Comp: check_and_mask
    Comp-->>Pipe: answer
    Pipe->>Redis: save_conversation_context
    Pipe->>MySQL: save_interaction_log
    Pipe-->>API: {answer, sources, intent, ...}
    API-->>Client: 200 + 流式/非流式
```

---

## 五、场景与模块映射表

| 业务场景 | 意图 | 专用模块 | 知识库来源 | 输出形式 |
|---------|------|----------|------------|----------|
| **通用保险咨询** | retrieval, consultation, claims, underwriting, product | 无 | RAGflow 主库 | 口语化 + 引用 |
| **百万医疗险推荐** | INTENT_MEDICAL_INSURANCE | extract_insurance_slots, Slot Filling 反问 | RAGflow 主库 | 产品列表 + 推荐理由 |
| **保障重叠度透视镜** | INTENT_COVERAGE_OVERLAP | extract_coverage_slots, compute_coverage_gap | RAGflow 主库 + 保障知识 | 重叠表 + 买/不买/换建议 |
| **医疗条款解析与咨询** | INTENT_CLAUSE_PARSE | clause_context, 条款上传/入库 | 用户条款库 + RAGflow 主库 | 条款解读 + 知识库补充 |
| **问候/其他** | greeting, other | 无 | 可选 | 简单回复 |

---

## 六、技术栈与目录结构

### 6.1 技术栈

| 层次 | 技术 |
|------|------|
| 后端 | Python 3.9+、FastAPI、Uvicorn |
| 配置 | pydantic-settings、.env |
| 数据库 | MySQL（用户、日志）、Redis（上下文） |
| 检索 | RAGflow（主知识库）、ChromaDB（规则向量）、可选 ES |
| 大模型 | 通义千问（DashScope），支持专业版/标准版 |
| 前端 | PC Web 静态页、Gradio、管理端 admin.html |
| 认证 | JWT、bcrypt |

### 6.2 核心目录结构

```
InsurGuide/
├── api/main.py              # FastAPI 入口、路由、静态资源
├── routers/                  # 认证、对话、条款、向量、ES、规则、管理
├── app/                      # 意图、改写、答案、合规、条款上下文、保障重叠、要素提取
├── services/rag/             # pipeline、pipeline_stream、recall、fusion、rerank
├── core/                     # MySQL、Redis、ChromaDB、ES、JWT
├── models/                   # User、InteractionLog、ComplianceLog
├── config/                   # settings、constants
├── gradio_ui/                # Gradio 演示（认证、对话、条款）
└── web/static/               # index.html、admin.html
```

---

## 七、API 路由总览

| 路由 | 功能 | 场景关联 |
|------|------|----------|
| POST /api/auth/register | 注册 | 全场景鉴权 |
| POST /api/auth/login | 登录 | 全场景鉴权 |
| GET /api/auth/me | 当前用户 | 全场景鉴权 |
| POST /api/chat | 对话（非流式） | 全场景 |
| POST /api/chat/stream | 对话（流式） | 全场景 |
| POST /api/chat/clear | 清除上下文 | 全场景 |
| GET /api/chat/history | 历史记录 | 全场景 |
| POST /api/chat/context/restore | 恢复历史+条款 | P2-11 |
| POST /api/clause/upload | 条款上传 | 条款解析 |
| GET /api/clause/context | 条款上下文 | 条款解析 |
| POST /api/clause/clear | 清除条款 | 条款解析 |
| /api/vector/* | 向量库 | 意图/改写规则 |
| /api/es/* | ES | 可选检索 |
| /api/intent-rules/* | 意图规则 | llm_vector 模式 |
| /api/admin/* | 管理看板 | P1-7 |

---

## 八、数据存储与流转

```mermaid
flowchart LR
    subgraph 写入["写入"]
        W1[用户注册/登录 → users]
        W2[每轮问答 → interaction_logs]
        W3[违规屏蔽 → compliance_logs]
        W4[对话历史 → Redis context]
        W5[条款上下文 → Redis clause_ctx]
    end

    subgraph 存储["存储"]
        M[(MySQL)]
        R[(Redis)]
    end

    subgraph 读取["读取"]
        R1[会话上下文]
        R2[条款上下文]
        R3[历史恢复]
    end

    W1 --> M
    W2 --> M
    W3 --> M
    W4 --> R
    W5 --> R
    R --> R1 & R2 & R3
```

---

## 九、P0 / P1 / P2 功能在架构中的位置

| 阶段 | 功能 | 架构位置 |
|------|------|----------|
| **P0** | 条款解析闭环：上传、解析、多轮咨询 | routers/clause、app/clause_context、RAGflow 多库检索 |
| **P0** | 前端条款状态栏、clause_loaded 展示 | web/static/index.html、routers/chat |
| **P1-5** | 条款粘贴入口 | web 前端、chat 请求 clause_text |
| **P1-6** | 条款结构化抽取 | app 层、前端卡片展示 |
| **P1-7** | 管理端看板 | routers/admin、/static/admin.html |
| **P1-8** | clause_loaded 流式展示 | answer_engine、chat 响应 |
| **P2-10** | Gradio 条款解析入口 | gradio_ui/pages/chat |
| **P2-11** | 历史恢复 clause 关联 | POST context/restore、Redis clause_ctx |
| **P2-12** | 智能推荐问题 | 条款加载后推荐问题、前端展示 |

---

## 十、文档索引

| 文档 | 说明 |
|------|------|
| [项目架构说明.md](../项目架构说明.md) | 整体架构、流程图、技术栈、依赖、代码结构 |
| [架构说明.md](../架构说明.md) | 分层与目录职责、RAG 流水线、扩展性 |
| [智保灵犀·医疗条款解析与咨询_可执行产品方案.md](../产品需求/智保灵犀·医疗条款解析与咨询_可执行产品方案.md) | 条款解析场景 |
| [智保灵犀·保障重叠度透视镜_可执行产品方案.md](../产品需求/智保灵犀·保障重叠度透视镜_可执行产品方案.md) | 保障重叠场景 |
| [智保灵犀_P0前端条款解析闭环_产品技术方案.md](../产品需求/智保灵犀_P0前端条款解析闭环_产品技术方案.md) | P0 闭环 |
| [智保灵犀_P1高价值功能增强_产品技术方案.md](../产品需求/智保灵犀_P1高价值功能增强_产品技术方案.md) | P1 增强 |
| [智保灵犀_P2加分项_产品技术方案.md](../产品需求/智保灵犀_P2加分项_产品技术方案.md) | P2 加分项 |

---

*本文档为智保灵犀全部场景的一体化架构视图，便于研发、产品与汇报使用。*
